class Solution {
    public boolean isSubsequence(String s, String t) {
        int left=0;
        int right=0;
        while(left < s.length() && right<t.length()){
            //ch(leftin s)== right in ch in t->left+= right++
            if(s.charAt(left)==t.charAt(right)){
                left++;
                right++;
            }
            else{
                right++;
            }
        }
        return left==s.length();
    }
}
